# ======================
# SERVER CONFIGURATION
# ======================
NODE_ENV=production
PORT=8080

# ======================
# DATABASE (AWS RDS)
# ======================
# For AWS RDS PostgreSQL, use the full connection string
# Format: postgresql://username:password@endpoint:5432/database_name
DATABASE_URL=postgresql://esignature_user:YourSecurePassword123@your-rds-instance.abc123.us-east-1.rds.amazonaws.com:5432/esignature_db

# Legacy individual database configs (optional, DATABASE_URL is preferred)
DB_HOST=your-rds-instance.abc123.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=esignature_db
DB_USER=esignature_user
DB_PASSWORD=YourSecurePassword123

# ======================
# JWT CONFIGURATION
# ======================
# Generate secure random strings for production
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-random-secret-key-minimum-32-characters-change-in-production
JWT_EXPIRE=7d
JWT_REFRESH_SECRET=your-random-refresh-secret-key-minimum-32-characters-change-in-production
JWT_REFRESH_EXPIRE=30d

# ======================
# AWS S3 CONFIGURATION
# ======================
# AWS Region where your S3 bucket is located
AWS_REGION=us-east-1

# AWS IAM credentials with S3 access
# Create IAM user with AmazonS3FullAccess policy
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# S3 bucket name for document storage
AWS_S3_BUCKET=esignature-documents-prod

# ======================
# EMAIL CONFIGURATION
# ======================
# SMTP Configuration (Gmail example)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com

# For Gmail, use App Password (not your regular password)
# Enable 2FA, then create App Password at: https://myaccount.google.com/apppasswords
EMAIL_PASSWORD=your-16-character-app-password

# Email sender address
EMAIL_FROM=noreply@yourdomain.com

# ======================
# FRONTEND URL
# ======================
# For production, use your Elastic Beanstalk URL or custom domain
CLIENT_URL=http://your-app-name.us-east-1.elasticbeanstalk.com

# Or with custom domain:
# CLIENT_URL=https://app.yourdomain.com

# ======================
# SECURITY
# ======================
# Bcrypt password hashing rounds (10-12 recommended)
BCRYPT_ROUNDS=10

# ======================
# RATE LIMITING
# ======================
# Window in milliseconds (900000 = 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Max requests per window
RATE_LIMIT_MAX_REQUESTS=100

# ======================
# FILE UPLOAD
# ======================
# Max file size in bytes (10485760 = 10MB)
MAX_FILE_SIZE=10485760

# Allowed MIME types (comma-separated)
ALLOWED_FILE_TYPES=application/pdf

# ======================
# AWS ELASTIC BEANSTALK NOTES
# ======================
# After deploying, set environment variables using:
# eb setenv NODE_ENV=production DATABASE_URL="postgresql://..." JWT_SECRET="..." [etc]
#
# Or use AWS Console:
# 1. Go to Elastic Beanstalk Console
# 2. Select your environment
# 3. Configuration > Software > Environment properties
# 4. Add all environment variables listed above
#
# NEVER commit actual credentials to version control!
# ======================
